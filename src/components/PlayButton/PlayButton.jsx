import styles from './PlayButton.module.css'
const PlayButton = () => (
  <div className={styles.container}>
    <button className={styles.button}>Launch Minecraft</button>
  </div>
)

export default PlayButton;
